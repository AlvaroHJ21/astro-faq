---
import Layout from '../layouts/Layout.astro';
import Card from '../components/CardItem.vue';
import faq from '../data/faq';
import Search from '../icons/Search.vue';
import CardList from '../components/CardList.vue';
---

<Layout title="Welcome to Astro.">
  <main class="mx-auto w-[90%] max-w-screen-sm py-8">
    <h1 class="text-3xl font-black text-center mb-8">FAQ</h1>

    <!-- <div class="mb-4 m-auto mr-0 w-fit">
      <div class="p-2 rounded-md border flex items-center gap-1">
        <Search color="#cecece" />
        <input type="text" class="outline-none" />
      </div>
    </div>
    <ul class="flex flex-col gap-2">
      {faq.map((item, index) => <Card answer={item.answer} question={item.question} />)}
    </ul> -->
    <CardList client:load/>
  </main>
</Layout>
